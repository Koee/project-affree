# Environment template — copy to .env.demo / .env.staging / .env.costco and fill real values
# Never commit real .env files

BASE_URL=https://example.com
API_BASE_URL=https://example.com/api
ORDER_COOP_PASSWORD=changeme
COOP_PASSWORD=changeme
API_PRODUCTS_ENDPOINT=/products
API_SEARCH_ENDPOINT=/search
API_COMPARE_ENDPOINT=/compare